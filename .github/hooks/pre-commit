actionlint
EXIT_CODE=$?
 
if [ $EXIT_CODE -ne 0 ]; then
  echo "Commit annule : workflow GitHub Actions invalide."
  exit 1
fi
 
# 3. Donner les droits d'exécution
chmod +x .git/hooks/pre-commit